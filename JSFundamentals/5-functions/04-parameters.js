


function hi(name , age){                     //anything in the () is a parameter, name sake for any data when you send the function.
    console.log(`Hi ${name}, you are ${age}!`);
}

hi("Mara", 20);      //These () are the argument for the parameter.


//challenge

function name(firstName, lastName) {
    let fullName = firstName + " " + lastName;
    console.log(`Hello, my name is ${fullName}`);
}

name("Mariah" , "Curl");